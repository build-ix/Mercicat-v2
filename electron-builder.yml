appId: com.nousresearch.mercicat
productName: Mercicat v2
artifactName: mercicat-v2-windows.${ext}
directories:
  output: release
  buildResources: build
files:
  - dist-electron/**
  - apps/client/dist/**
  - package.json
asar: true
win:
  target:
    - target: portable
      arch:
        - x64
  artifactName: mercicat-v2-windows.${ext}
