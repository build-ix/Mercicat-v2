import type { GameState } from "@mercicat/shared";
export function getSpawnPacing(_state: GameState): { intervalTicks: number; maxActiveEnemies: number } { return { intervalTicks: 0, maxActiveEnemies: 0 }; }
