import type { Server, Socket } from "socket.io";
import type { RoomManager } from "../roomManager.js";
export function registerInputHandlers(_io: Server, _rooms: RoomManager, _socket: Socket): void { /* wired by serverLifecycle */ }
